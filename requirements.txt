# --- Deep learning ---
tensorflow==2.19.0
keras==3.10.0

# --- Machine vision ---
numpy>=1.22.4,<2.0
opencv-python>=4.8.0,<5.0

# --- Web / services ---
flask==3.0.0
requests==2.31.0
flask-cors==4.0.0

# --- Detection models ---
ultralytics>=8.0.28,<9.0

# --- PyTorch ---
torch>=2.0.0
torchvision>=0.15.0

# --- Data / utilities ---
matplotlib==3.7.3
pandas>=1.5.0,<3.0
scikit-learn>=1.2.0
Pillow>=9.0.0
scipy>=1.10.0
tqdm>=4.64.0
pyyaml>=6.0

# --- Interactive (optional, for notebooks) ---
ipython

# --- Simulator clients (install separately as needed) ---
# carla: NOT on PyPI. Install manually from CARLA release wheel:
#   pip install <CARLA_ROOT>/PythonAPI/carla/dist/carla-*.whl
# beamngpy: requires a BeamNG.tech licence.
#   pip install beamngpy